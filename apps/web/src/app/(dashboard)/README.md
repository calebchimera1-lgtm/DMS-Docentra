# (dashboard) route group

Authenticated shell (sidebar, topbar) and the dashboard home screen — added in Milestone 5 (Dashboard). Per-module routes (`crm/`, `sales/`, `inventory/`, ...) are added one at a time from Milestone 7 onward.
