export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 p-8 text-center">
      <h1 className="text-4xl font-bold tracking-tight">Omniflow</h1>
      <p className="max-w-xl text-muted-foreground">
        Modular enterprise ERP platform — under active, incremental
        construction. Folder structure and app skeletons are in place;
        the database schema, authentication, and modules land next.
      </p>
    </main>
  );
}
