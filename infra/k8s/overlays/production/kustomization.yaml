apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: omniflow
resources:
  - ../../base

# Production-specific patches (replica counts, resource limits, ingress
# host/TLS, HPA thresholds) land alongside the CI/CD pipeline in Milestone 6.
