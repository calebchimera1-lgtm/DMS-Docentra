# API documentation

The REST API is self-documented via OpenAPI/Swagger, served at
`/api/docs` by `apps/api` (see `apps/api/src/main.ts`). This directory
holds supplementary reference material (auth flows, pagination/filtering
conventions, webhook payloads, GraphQL schema notes) as those land with
Milestones 3, 4, and 6.
