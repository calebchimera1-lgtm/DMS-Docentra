/**
 * Seed script placeholder — populated once the schema (Milestone 2) lands.
 */
async function main(): Promise<void> {
  console.log("No seed data yet — schema lands in Milestone 2.");
}

main();
